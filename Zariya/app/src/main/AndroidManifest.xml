<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.mgnrega.mgnregaapp">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/nrega"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Startup"
            android:icon="@drawable/nrega"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        .
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Signup"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login_official" />
        <activity android:name=".WorkerApplicationForm" />
        <activity android:name=".Display_Application_Data" />
        <activity
            android:name=".FingerprintActivity"
            android:icon="@drawable/nrega" />
        <activity android:name=".about" />
        <activity android:name=".Home" />
        <activity android:name=".contact" />
        <activity android:name=".worksite" />
        <activity android:name=".AllotWork"></activity>
    </application>

</manifest>